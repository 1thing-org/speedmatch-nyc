## How to Run Locally

### Prerequisites

- Node and npm installed (Instructions are in https://nodejs.org/en)
- Yarn
```bash
npm install -g yarn
```

### Steps

```bash
# Clone the repository
git clone <repo-url>
cd speedmatch-nyc

# Install project dependencies
yarn

# Start the development server
yarn dev
